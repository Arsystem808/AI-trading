#!/usr/bin/env python3
"""
AlphaPulse Strategy Individual Backtest
Tests AlphaPulse strategy alone (no ensemble)
"""

# [Identical structure to backtest_m7.py, but replace:]
# - analyze_asset_m7 → analyze_asset_alphapulse
# - "M7" → "AlphaPulse" in all strings
# - "m7_backtest" → "alphapulse_backtest" in filename

# Import section:
from core.strategy import (
    analyze_asset_alphapulse,  # Changed from analyze_asset_m7
    _atr_like,
    logger
)

# In run_alphapulse_backtest() function:
def run_alphapulse_backtest():
    print("🚀 AlphaPulse Strategy Individual Backtest\n")
    
    print(f"""
📝 Configuration:
   - Strategy: AlphaPulse ONLY (fundamental + ML)
   - Market orders (entry at next day open)
   [... same config ...]
""")
    
    # ... identical logic but call analyze_asset_alphapulse:
    alpha_signal = analyze_asset_alphapulse(ticker, "Краткосрочный")
    
    # ... rest same ...
    
    output_file = output_dir / f"alphapulse_backtest_{timestamp}.json"
    
    # ... rest identical ...

if __name__ == "__main__":
    run_alphapulse_backtest()
